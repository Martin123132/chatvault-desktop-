# Reconstruction Notes

The project has been flattened to a standard top-level layout:
- `src/` for Python source modules
- `templates/` for HTML templates
- `static/` for static assets
- `requirements.txt` at repo root
- `chatvault.py` as the CLI entrypoint

Legacy nested reconstruction folders were removed after flattening.
